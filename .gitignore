# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Env (committed: .env.example only)
.env
.env.local
.env.*.local
*/.env
*/.env.local
*/.env.*.local

# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.next/
out/
*.tsbuildinfo

# Prisma
*.db
*.db-journal

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/

# Coverage
coverage/
.nyc_output/

# Flutter (Phase 5.5+)
mobile/.dart_tool/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/.packages
mobile/build/
mobile/.idea/
mobile/.gradle/
mobile/.metadata
mobile/ios/Pods/
mobile/ios/.symlinks/
mobile/ios/Flutter/Flutter.framework
mobile/ios/Flutter/Flutter.podspec
mobile/android/.gradle/
mobile/android/captures/
mobile/android/local.properties

# Docker
docker-compose.override.yml

# Misc
.cache/
tmp/

# Phase 5c·D — local-driver uploads (logos, cover images). Never commit user content.
api/storage/

# Phase 5.5·A4 — openapi_generator_cli writes a 30 MB JAR cache + config to
# whichever cwd you ran `dart pub global activate` from. Keep them out of the
# repo — the codegen runner uses ~/.pub-cache which is already user-local.
.dart_tool/
openapi_generator_config.json
mobile/openapi_generator_config.json
